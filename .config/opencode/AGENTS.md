# Global OpenCode Rules

## File Editing
- ALWAYS use the Read tool on a file before using Edit or Write on it, even if you think you already know its contents.
- If an Edit fails with "file has been modified since last read", re-read the file immediately before retrying the edit.
- Never assume file contents from memory — always read first.
