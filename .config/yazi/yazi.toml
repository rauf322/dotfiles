# Only overrides of yazi's built-in defaults live here; everything else is inherited.
"$schema" = "https://yazi-rs.github.io/schemas/yazi.json"

[mgr]
ratio = [ 2, 4, 3 ]

[preview]
max_width  = 1200
max_height = 1000
