#!/usr/bin/env bash
set -euo pipefail
DIR="${1:-$(tmux display -p -F "#{pane_current_path}")}"

if command -v zed >/dev/null 2>&1; then
  # open directory in Zed
  zed "$DIR" >/dev/null 2>&1
else
  echo "zed CLI not found on PATH" >&2
  exit 127
fi

